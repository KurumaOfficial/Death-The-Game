/**
 * Ядро игрового цикла, сцен, сервисов, конфига и сейвов.
 *
 * <p>Этот пакет содержит конкретные реализации контрактов, объявленных в
 * {@code WeTTeA.api}. Здесь живёт всё, что не зависит от конкретной платформы
 * и render API: главный {@code GameApp}, {@code GameLoop}, менеджер сцен,
 * сервисный контейнер, конфигурация качества рендера, сериализация сейвов.
 *
 * <p>Запрещено в этом пакете:
 * <ul>
 *   <li>обращение к LWJGL/GLFW/Vulkan напрямую;</li>
 *   <li>использование Android/iOS специфичных типов;</li>
 *   <li>смешивание render логики с core логикой;</li>
 *   <li>хранение Vulkan handles или Rust pointer'ов в core объектах.</li>
 * </ul>
 *
 * @author Kuruma
 * @since 0.1.0
 */
package WeTTeA.core;
