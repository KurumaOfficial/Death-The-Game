/**
 * Высокоуровневые render-помощники (sprite batch, debug draw, скайбокс).
 *
 * <p>Слой стоит над {@link WeTTeA.api.render.RenderBackend} и не знает
 * про конкретный API (Vulkan/GL).
 *
 * <p>На этапе 1 — INTEGRATION_MISSING.
 *
 * @author Kuruma
 * @since 0.1.0
 */
package WeTTeA.render;
